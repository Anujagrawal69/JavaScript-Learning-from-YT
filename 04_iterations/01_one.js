// const arr = [2,3,4,5,6]
// for (const value of arr) {
//     console.log(value);
// }


const map = new Map()
map.set("1", "one")
map.set("2", "two")
map.set("3", "three")

// for (const set of map) {
    // console.log(set);
// }

// for (const [key, value] of map) {
//     console.log(key, " -> ", value);
// }

// but we can't iterate objects using forof loop, instead we've other ways.

